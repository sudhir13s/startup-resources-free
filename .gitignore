# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.installed.cfg
*.egg
.venv/
.venv-*/
venv/
venv-*/
env/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Node / Next.js
node_modules/
.next/
out/
dist/
.turbo/
.vercel/
.swc/
*.tsbuildinfo
next-env.d.ts

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Local data (only fixtures + data/providers_seed.json committed — the DB
# lives on the `data` git branch / Render's ephemeral /tmp, never `main`)
data/raw/
data/logs/
data/runs/
data/verify-queue/
data/freellm/quotas.json
data/search/quotas.json
data/benchmark_runs/
data/*.db

# Editor / OS / temp
.DS_Store
*.bak
*.swp
.idea/
.vscode/
*.tmp

# Internal working files — kept locally, never published.
# Public docs live in architecture/ and README.md only.
.claude/
CLAUDE.md
BACKLOG.md
project-idea.md
docs/
# SQLite write-ahead log + shared-memory/journal files — ephemeral, never
# commit (the .db itself is also ignored above; in production it lives in
# a Cloudflare R2 bucket, synced by api/ via storage/r2_sync.py).
data/resourceos.db-wal
data/resourceos.db-shm
data/resourceos.db-journal
!data/raw/_fixtures/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/*
!.vscode/settings.json.example
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Cache
.cache/
.eslintcache
.stylelintcache

# Render local
.render/

# Background-task transcripts (claude-code internal)
/tmp/claude-*

# pnpm
.pnpm-store/
