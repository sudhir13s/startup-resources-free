-r requirements.txt
# Agent stack — used by the daily cron pipeline + local dev that drives
# real LLM calls through freellm's OpenAI-compatible httpx backend. NOT
# installed in CI (MockBackend covers all freellm tests without a real
# network call). Deliberately no OmniRoute / Node sidecar / heavy SDK —
# freellm must fit a 512 MB Render free web service.
httpx==0.28.1
# Sprint #6 — search-chain agent + aggregator scraping. selectolax is a
# fast HTML parser (lxml-bound C). All search providers are called via
# httpx directly — no per-provider SDK to keep this list short.
selectolax==0.3.21
PyYAML==6.0.2
