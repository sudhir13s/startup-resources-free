-r requirements.txt
# Agent stack — used by the daily cron pipeline + local dev that drives
# real LLM calls through OmniRoute. NOT installed on Render (Render serves
# the DB only) and NOT installed in CI (MockBackend covers all freellm
# tests without spinning OmniRoute).
#
# OmniRouteConfig pin: matches the commit verified locally on 2026-04-27.
OmniRouteConfig @ git+https://github.com/sudhir13s/OmniRouteConfig.git@7bdce4dbdbde5d0c3d511f1f54e4ee660099c1b9
openai==2.36.0
httpx==0.28.1
# Sprint #6 — search-chain agent + aggregator scraping. selectolax is a
# fast HTML parser (lxml-bound C). All search providers are called via
# httpx directly — no per-provider SDK to keep this list short.
selectolax==0.3.21
PyYAML==6.0.2
