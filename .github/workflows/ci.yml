name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python:
    name: python lint + tests (backend + freellm)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12.3"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt
      - name: install dev deps
        run: pip install -r backend/requirements-dev.txt
      - name: ruff
        run: ruff check backend/ freellm/
      - name: pytest
        run: pytest backend/tests freellm/tests -v
      - name: freellm CLI smoke
        run: |
          python -m freellm version
          python -m freellm catalog
          python -m freellm catalog --modality text
          python -m freellm keys

  build-frontend:
    name: frontend lint + build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: "20.12.2"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: install
        working-directory: frontend
        run: npm install --no-audit --no-fund
      - name: lint
        working-directory: frontend
        run: npm run lint
      - name: build
        working-directory: frontend
        env:
          BACKEND_URL: http://localhost:8000
          NEXT_PUBLIC_APP_ENV: ci
        run: npm run build
