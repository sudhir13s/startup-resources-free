"""Domain layer — the canonical shapes every other module speaks.

Pure: no I/O, no network, no imports from other project packages.
`storage/`, `refresh/`, `api/` depend on this package; never the reverse.
"""
